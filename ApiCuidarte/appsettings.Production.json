{
  "ConnectionStrings": {
    "cuidarte": "Host=${DB_HOST};Port=${DB_PORT};Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD}"
  },
  "Jwt": {
    "SecretKey": "${JWT_SECRET}"
  },
  "Email": {
    "FromName": "${EMAIL_FROMNAME}",
    "FromAddress": "${EMAIL_FROMADDRESS}",
    "SmtpHost": "smtp.gmail.com",
    "SmtpPort": 587,
    "Password": "${EMAIL_PASSWORD}"
  }
}